import { ToWordsPipePipe } from './to-words-pipe.pipe';

describe('ToWordsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ToWordsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
